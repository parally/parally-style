import * as React from 'react'

class Button extends React.Component {
  render() {
    return (
      <button>
        Test Button
        {this.props.children}
      </button>  
    )
  }
}

export default Button
